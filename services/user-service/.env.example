# ========================================
# BennyCar User Service Environment Variables
# ========================================
# IMPORTANT: This is a TEMPLATE file (.env.example)
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp .env.example .env
# 2. Fill in the actual values in .env
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. Always keep .env.example updated with new variables
# ========================================

# ========================================
# Spring Profile Configuration
# ========================================
# Options: dev, prod, test
# - dev: Development environment (verbose logging, auto-update schema)
# - prod: Production environment (minimal logging, strict validation)
SPRING_PROFILES_ACTIVE=dev

# ========================================
# Server Configuration
# ========================================
SERVER_PORT=8081

# ========================================
# Database Configuration
# ========================================
# PostgreSQL connection settings
# Format: jdbc:postgresql://host:port/database?currentSchema=schema_name
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/bennycar_db?currentSchema=user_service
SPRING_DATASOURCE_USERNAME=admin
SPRING_DATASOURCE_PASSWORD=your_secure_password_here

# JPA/Hibernate Settings
# Options: validate, update, create, create-drop
# - validate: Only validate schema (RECOMMENDED for production)
# - update: Update schema automatically (OK for development)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# ========================================
# JWT Configuration
# ========================================
# CRITICAL: Use a strong, random secret key (minimum 256 bits / 32 characters)
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-very-long-secret-key-change-this-min-256-bits-use-openssl-rand-base64-32
JWT_EXPIRATION=86400000

# ========================================
# Logging Configuration (Optional)
# ========================================
# Logging levels: TRACE, DEBUG, INFO, WARN, ERROR, OFF
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG
LOG_LEVEL_SECURITY=DEBUG
LOG_LEVEL_SQL=DEBUG

# Hibernate SQL formatting
HIBERNATE_FORMAT_SQL=true
JPA_SHOW_SQL=false

# ========================================
# Actuator Configuration (Optional)
# ========================================
# Options: always, when-authorized, never
ACTUATOR_HEALTH_DETAILS=always

# ========================================
# NOTES FOR PRODUCTION:
# ========================================
# 1. Use strong, randomly generated passwords
# 2. Use a proper secret management service (AWS Secrets Manager, Azure Key Vault, etc.)
# 3. Set SPRING_PROFILES_ACTIVE=prod
# 4. Set SPRING_JPA_HIBERNATE_DDL_AUTO=validate
# 5. Use environment variables directly (don't use .env files in production)
# 6. Rotate JWT secrets regularly
# 7. Use HTTPS in production
# ========================================

